const { compare } = require("bcrypt");

const saltKey = "njjerufndsk"; //not real

const password= "test";

const alg = (password) => hash 
alg (saltKey + password);

//compare (hash) => Boolean
